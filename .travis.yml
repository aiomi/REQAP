language: python
sudo: required
python:
- '3.6'
branches:
  only:
  - master
install:
- pip install -r requirements.txt
script:
- python manage.py --help

deploy:
  provider: heroku
  api_key: "$HEROKU_API_KEY"
  app: "$HEROKU_APP_NAME"

